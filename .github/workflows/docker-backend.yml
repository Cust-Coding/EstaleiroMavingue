name: Build and Push Backend Docker

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build Docker image
        run: |
          cd apps/api/auth/app
          docker build -t custcoding/estaleiro-backend:latest .

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push Docker image
        run: |
          docker tag custcoding/estaleiro-backend:latest ${{ secrets.DOCKER_USERNAME }}/estaleiro-backend:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/estaleiro-backend:latest